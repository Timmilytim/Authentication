server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  email:
    host: smtp.example.com
    port: 587
    username: email@gmail.com
    password: ghak nbkz fbsv rvkt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/register
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        show_sql: true
